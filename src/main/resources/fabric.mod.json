{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "Type :item_id: on a sign or hanging sign and it renders as the item's in-game icon, Bedrock-style. Purely visual/client-side - the raw text stays as-is, so players without the mod just see the plain text.",
  "authors": [
    "orbital"
  ],
  "contact": {
    "issues": "https://github.com/yourname/signicons/issues"
  },
  "license": "${mod_license}",
  "environment": "client",
  "entrypoints": {
    "client": [
      "com.orbital.signicons.SignIcons"
    ]
  },
  "mixins": [
    "signicons.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "~${minecraft_version}",
    "fabric-api": "*"
  }
}
